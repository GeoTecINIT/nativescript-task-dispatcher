export declare class BootReceiver implements es.uji.geotec.taskdispatcher.BootReceiver {
    onReceive(context: android.content.Context, intent: android.content.Intent): void;
    private handleOnReceive;
}
